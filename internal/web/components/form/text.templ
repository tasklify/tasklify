package form

templ TextField(label, inputID, value, placeholder string) {
	<div class="form-control">
		<label class="label">
			<span class="label-text">{ label }</span>
		</label>
		<input required value={ value } type="text" name={ inputID } id={ inputID } placeholder={ placeholder } class="input input-bordered"/>
	</div>
}
